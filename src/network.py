# network.py
import layer

class Network:
    def __init__(self, layers: list):
        # Example for layers=[2,3,4]
        #   There will be 3 layers. Layer 1 has 2 nodes, Layer 2 has 3 nodes, Layer 3 has 4 nodes
        #   The first layer (input layer) will takes in 2 inputs and gives 2 outputs.
        #   The second layer (part of the hidden layers) takes 2 inputs and gives 3 outputs.
        #   The third layer (output layer) takes in 3 inputs and gives 4 outputs
        pass